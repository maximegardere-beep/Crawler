name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      # Pas de "npm install" : package.json n'a aucune dependency/devDependency (voir CLAUDE.md,
      # Conventions de travail — aucun framework de test, aucun build step).
      - run: npm test
      - run: npm run test:long
